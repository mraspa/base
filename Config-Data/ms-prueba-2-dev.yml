server:
  port: 8082

management:
  endpoints:
    web:
      exposure:
        include: info
  info:
    env:
      enabled: true
    app:
      name: ms-prueba-2
      version: 1.0.0
      description: Microservicio Prueba 2

info:
  app:
    name: ${spring.application.name}